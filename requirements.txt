# =========================================
# Annual Report Workspace (V2) Dependencies
# =========================================

# ---- Presentation & State Management ----
streamlit>=1.32.0         # Core UI framework and session state management

# ---- Document Processing ----
PyMuPDF>=1.23.0           # PDF text extraction, bounding-box math, and byte-splitting
fpdf2>=2.8.0              # Generate PDF documents from chat history

# ---- LLM Inference Abstraction ----
google-generativeai>=0.4.0 # Google Gemini SDK for chat, embeddings, and content generation
# pydantic>=2.0.0         # (Optional) Uncomment if structured LLM outputs or robust config validation are needed

# ---- Security & Environment ----
python-dotenv>=1.0.0      # Secure local loading of Gemini API keys via .env files

# ---- Testing & Quality Assurance ----
# Dependencies required to run the `tests/` directory
pytest>=7.4.0             # Core testing framework
pytest-mock>=3.12.0       # Mocking PyMuPDF and LLM Network calls during testing
